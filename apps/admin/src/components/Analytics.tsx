import { useEffect, useState } from "react";
import { API_BASE_URL } from "../config/api";
type Analytics = { revenue: number; paidOrders: number; averageOrderValue: number; purchasingCustomers: number; newCustomers: number; returningCustomers: number };
const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);
export default function Analytics() { const [data,setData]=useState<Analytics|null>(null); const [error,setError]=useState(""); useEffect(()=>{void(async()=>{try{const r=await fetch(`${API_BASE_URL}/admin/dashboard/analytics`,{credentials:"include"});const result=await r.json();if(!r.ok||!result.success)throw new Error(result.message);setData(result.data);}catch(e){setError(e instanceof Error?e.message:"Unable to load analytics");}})();},[]); const cards=data?[['Paid revenue',money(data.revenue)],['Paid orders',String(data.paidOrders)],['Average order value',money(data.averageOrderValue)],['Purchasing customers',String(data.purchasingCustomers)],['New customers',String(data.newCustomers)],['Returning customers',String(data.returningCustomers)]]:[]; return <section className="mt-6 rounded-2xl border border-black/10 bg-white p-4 shadow-sm sm:p-6"><p className="text-xs font-semibold uppercase tracking-[.2em] text-[#C9A227]">Performance</p><h2 className="mt-1 text-xl font-semibold">Business analytics</h2><p className="mt-1 text-sm text-black/60">Paid-order revenue, average order value and customer retention.</p>{error?<p className="mt-5 text-sm text-red-600">{error}</p>:<div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([label,value])=><div key={label} className="rounded-xl border border-black/10 bg-[#FFF9ED] p-4"><p className="text-xs uppercase tracking-wide text-black/50">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>)}</div>}</section>; }
